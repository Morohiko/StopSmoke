<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Initial Allowed Cigarettes -->
        <TextView
            android:text="@string/initial_allowed_cigarettes_per_day"
            android:textSize="16sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <EditText
            android:id="@+id/editTextInitialCigs"
            android:inputType="number"
            android:hint="@string/e_g_20"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp" />

        <!-- Reduction Rate -->
        <TextView
            android:text="@string/reduction_rate_cigarettes_per_week"
            android:textSize="16sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <EditText
            android:id="@+id/editTextReductionRate"
            android:inputType="number"
            android:hint="@string/e_g_1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp" />

        <!-- Save Settings Button -->
        <Button
            android:id="@+id/buttonSaveSettings"
            android:text="@string/save_settings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp" />

        <!-- Clear Cache and History Button -->
        <Button
            android:id="@+id/buttonClearCacheHistory"
            android:text="@string/clear_cache_and_history"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/design_default_color_error"
            android:textColor="@color/white"
            android:layout_marginBottom="16dp" />

    </LinearLayout>
</ScrollView>
